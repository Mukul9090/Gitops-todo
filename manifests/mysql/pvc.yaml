apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: todo-app       # ← this line must be there!
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi